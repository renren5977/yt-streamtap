[project]
name = "yt-streamtap"
version = "0.1.0"
description = "yt stream tool"
dependencies = [
    "playwright",
    "portion",
    "diskcache",
    "sqlite-utils",
]

[tool.setuptools.packages.find]
include = ["yt_streamtap*"]

[project.scripts]
yts = "yt_streamtap.cli:cli"